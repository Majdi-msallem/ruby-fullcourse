Class Student

attr_reader :name, :email, :contact

def initialize(ame, email, contact)
  @name = name
  @email = email
  @contact = contact
end

  def email=(email)
   @email= email
  end
def name=(name)
  @name= name
end
def contact(contact)
  @contact= contact
end

end